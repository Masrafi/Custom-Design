

abstract class DropDownIMPL {
  Future dataShowFetch();
  
  /// for post method
  //Future<Either<Failure, List<DataView>>> dataShowFetch(
      //       {required int id, required DateTime fromDate, required DateTime toData});
}